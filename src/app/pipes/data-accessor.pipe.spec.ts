import { DataAccessorPipe } from './data-accessor.pipe';

describe('DataAccessorPipe', () => {
  it('create an instance', () => {
    const pipe = new DataAccessorPipe();
    expect(pipe).toBeTruthy();
  });
});
