import { FunctionAccessorPipe } from './function-accessor.pipe';

describe('FunctionAccessorPipe', () => {
  it('create an instance', () => {
    const pipe = new FunctionAccessorPipe();
    expect(pipe).toBeTruthy();
  });
});
